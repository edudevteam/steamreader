ignoredBuiltDependencies:
  - esbuild
  - sharp

# workerd ships the Workers runtime binary that `wrangler pages dev` executes,
# so unlike the two above it is useless without its install script.
onlyBuiltDependencies:
  - workerd
