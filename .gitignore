NOTES.md
supabase/.temp/

# wrangler and its local state now live under public-site/, which is the Pages
# project root -- see public-site/.gitignore. These stay only as a backstop for
# a stray run from the repo root.
.dev.vars
.wrangler/
node_modules/
